{
  "type": "capability",
  "name": "REST",
  "description": "Adds REST capability to the user's project",
  "props": {
    "runtime": {
      "name": "Runtime Type",
      "description": "The type of runtime to use",
      "required": true,
      "type": "enum",
      "values": [
        {
          "id": "nodejs",
          "name": "Node.js"
        },
        {
          "id": "springboot",
          "name": "Spring Boot"
        },
        {
          "id": "thorntail",
          "name": "Thorntail"
        },
        {
          "id": "vertx",
          "name": "Vert.x"
        }
      ]
    },
    "groupId": {
      "name": "Group Id",
      "description": "The Maven Group Id for the project",
      "required": true,
      "type": "string",
      "default": "org.openshift.appgen"
    },
    "artifactId": {
      "name": "Artifact Id",
      "description": "The Maven Artifact Id for the project",
      "required": true,
      "type": "string",
      "default": "my-app"
    },
    "version": {
      "name": "Version",
      "description": "The Maven Version for the project",
      "required": true,
      "type": "string",
      "default": "1.0"
    }
  }
}
